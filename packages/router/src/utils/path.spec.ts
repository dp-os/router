import { describe, it } from 'vitest';

import { normalizeLocation } from './path';

console.log(normalizeLocation({ path: '/' }));
console.log(normalizeLocation({ path: '/' }));
console.log(normalizeLocation({ path: '/' }));
console.log(normalizeLocation({ path: '/' }));
